# AGENTS.md

## プロジェクト概要

このリポジトリは、WordPressサイト `https://fuji-soho.net/` で使用する
オリジナルテーマ `fuji-soho-wp-theme` のリポジトリです。

fuji-sohoの事業サイトに加えて、複数のアプリの公式ページ、
プライバシーポリシー、利用規約、サポートページなどを管理します。

アプリごとに異なるデザイン、URL、文章、画像を使用します。
一方のアプリ固有の設定やデザインを、別のアプリへ流用しないでください。

---

## 対象ブランチ

作業前に現在のブランチを確認してください。

ユーザーから指定がある場合は、指定されたブランチを使用してください。

ブランチの切り替え、コミット、push、マージは、
ユーザーから明示的な依頼があった場合のみ行ってください。

---

## アプリ別指示書

作業対象に応じて、実装または提案を始める前に
次の指示書を確認してください。

| 作業対象 | 指示書 |
|---|---|
| ミミの冒険 | `docs/agents/mimis-adventure.md` |
| 飴玉工房 | `docs/agents/amedama-kobo.md` |

指示書がまだ作成されていないアプリについては、
既存アプリのデザインや仕様を流用せず、実装前にユーザーへ確認してください。

作業対象が判断できない場合は、推測せずユーザーへ確認してください。

アプリ別指示書とこのファイルが矛盾する場合は、
サイト全体の安全性や既存ページへの影響に関するルールを優先してください。

---

## 作業前の確認

変更や提案を行う前に、必ず既存実装を確認してください。

特に次を確認してください。

- `functions.php`
- `header.php`
- `footer.php`
- `page.php`
- 固定ページテンプレート
- テンプレートパーツ
- CSS・JavaScriptの読み込み方法
- アセットのディレクトリ構成
- レスポンシブ対応
- 使用中の命名規則

存在しないファイル、関数、CSSクラス、プラグインを
推測だけで使用しないでください。

---

## 共通実装方針

- 既存サイトのヘッダー、フッター、基本構造を可能な限り再利用する
- アプリ専用デザインは、そのアプリのページだけに適用する
- 他のアプリや既存の事業ページへ変更を波及させない
- 依頼されていない仕様変更やリファクタリングを行わない
- 最小限の変更で実装する
- 共通化できる基本構造と、アプリ固有のデザインを分離する
- 固定ページ本文はWordPress管理画面から編集できる構成を優先する
- 法的文章をPHPテンプレートへ直接記述しない
- 日本語版と英語版で内容上の矛盾を作らない

---

## アプリ間の分離

アプリごとに次の要素を分離してください。

- ページ判定
- CSSクラス
- CSSカスタムプロパティ
- 画像ディレクトリ
- 固有のテンプレートパーツ
- ページ内ナビゲーション
- 日本語・英語ページの対応関係

CSSはアプリ名を含む名前空間の配下に限定してください。

例：

```css
.app-page--mimis-adventure {
    /* ミミの冒険専用 */
}

.app-page--daughters-app {
    /* 娘のアプリ専用 */
}
```

`.page h2` や `.content img` など、
他ページへ影響する可能性がある広いセレクターを追加しないでください。

---

## 共通化の方針

次のような基本構造は、可能であれば共通化してください。

- アプリページ全体の基本レイアウト
- 日本語・英語切替
- プライバシーポリシーの本文コンテナ
- 利用規約の本文コンテナ
- サポートページの基本構造
- パンくずリスト
- アプリ内ナビゲーション
- アクセシビリティ対応

次の要素はアプリごとに設定可能にしてください。

- アプリ名
- 英語名
- URLスラッグ
- 配色
- フォント
- ロゴ
- キャラクター画像
- ヒーロー画像
- App Storeリンク
- ナビゲーション項目

将来のアプリ追加を考慮しますが、
現時点で不要な大規模フレームワークは作らないでください。

---

## WordPress実装ルール

- WordPress Coding Standardsを意識する
- 出力内容に応じて適切にエスケープする
- URLやテーマパスを直接固定しない
- CSSとJavaScriptは適切にenqueueする
- アプリ専用アセットは対象ページだけで読み込む
- WordPressコアファイルを変更しない
- 外部入力を信頼しない
- 管理処理にはnonceと権限確認を使用する
- PHPの警告やNoticeを発生させない

---

## 法的ページ

- 法的文章を独断で確定、削除、変更しない
- 使用している外部サービスを推測しない
- アプリごとのデータ取得内容を混同しない
- 制定日、改定日、連絡先を推測しない
- 日本語版を確定してから英語版へ反映する
- 不明点はユーザーへ確認する

AdMobなどを一方のアプリだけが使用している場合、
その記載を別アプリのプライバシーポリシーへ流用しないでください。

---

## 動作確認

変更後は次を確認してください。

- PHPの構文エラーがない
- 対象アプリの日本語・英語ページが表示できる
- 他のアプリページに変更が波及していない
- 既存のfuji-soho事業ページに変更が波及していない
- PCとスマートフォンでレイアウトが崩れない
- 長い日本語・英語文章でも表示が崩れない
- リンク切れがない
- 新しいブラウザコンソールエラーがない

---

## 作業結果の報告

変更後は次を簡潔に報告してください。

- 変更内容
- 変更ファイル
- 対象となったアプリ
- 実行した確認・テスト
- 未確認事項
- WordPress管理画面で必要な設定

## Git操作について

Gitに関する操作はユーザー側で行う。

Codexは、明示的に依頼されない限り、以下のGit操作を実行しないこと。

- `git add`
- `git commit`
- `git push`
- `git pull`
- `git merge`
- `git rebase`
- ブランチの作成・削除・切り替え
- タグの作成・削除
- その他、リポジトリの状態を変更するGit操作

実装や修正が完了した場合は、変更内容と確認結果を報告するところまでとし、
コミットやプッシュなどのGit操作は行わないこと。

`git status`、`git diff`、`git log`、`git branch` など、
リポジトリの状態を確認するための読み取り専用のGitコマンドは使用してよい。
